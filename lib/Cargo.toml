[package]
name = "matters_lib"
version = "0.1.0"
edition = "2021"

[dependencies]
rand = { workspace = true, default-features = false }
clap = { workspace = true, optional = true }

[features]
default = []
clap = ["dep:clap"]
ffi = []
